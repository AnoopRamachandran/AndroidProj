<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anoop_ramachandran.menu" >



    <application
        android:allowBackup="true"
        android:icon="@mipmap/_logo"
        android:label="@string/app_name"
        android:theme="@style/LightTheme"
        android:largeHeap="true">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity"></activity>

        <activity android:name=".ApplicationActivity"
            android:theme="@style/Theme.AppCompat.Light">
        </activity>

        <activity android:name=".ReferalActivity"></activity>

        <activity android:name=".CreateAccountActivity"></activity>

        <activity android:name=".MobileVerificationActivity"></activity>

        <activity android:name=".UniqIDGenActivity"></activity>

        <activity android:name=".UserAddressInfActivity"></activity>

    </application>

</manifest>
